# expandSparse: Convert sparse matrix to dense data frame

Expands a sparse matrix to a dense data frame

## Usage

``` r
expandSparse(sparsedMatrix)
```

## Arguments

- sparsedMatrix:

  a sparse matrix to convert

## Value

a dense data frame
