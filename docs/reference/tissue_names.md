# tissue_names: Prints the names of the tissue samples in the STlist

returns a character vector with the names of tissue samples in the
STlist.

## Usage

``` r
tissue_names(x)
```

## Arguments

- x:

  an STList object

## Value

a character vector with the sample names in the STlist object
