% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/STdiff_spatial.R
\name{STdiff_extract_spatial_pvalues}
\alias{STdiff_extract_spatial_pvalues}
\title{STdiff_extract_spatial_pvalues: Extract spatial p-values from model results}
\usage{
STdiff_extract_spatial_pvalues(
  sp_models = NULL,
  meta_dict = NULL,
  combo_df = NULL,
  pairwise = NULL,
  pval_adj = "fdr"
)
}
\arguments{
\item{sp_models}{list of spatial models from STdiff_run_spatial()}

\item{meta_dict}{annotation dictionary from STdiff_select_genes()}

\item{combo_df}{combinations dataframe from STdiff_select_genes()}

\item{pairwise}{whether tests are pairwise (TRUE) or reference-based (FALSE)}

\item{pval_adj}{p-value adjustment method (default: 'fdr')}
}
\value{
data frame with columns:
\itemize{
\item sample: sample name
\item gene: gene name
\item cluster_1: first cluster (original annotation name)
\item cluster_2: second cluster (only if pairwise=TRUE)
\item exp_p_val: raw spatial p-value
\item exp_adj_p_val: adjusted spatial p-value
\item exp_estimate: coefficient estimate for spatial effect
\item convergence_status: model convergence status
\item comments_spatial: warning/error message if any
}
}
\description{
Extracts spatial p-values and associated statistics from fitted
spatial models, handling convergence failures and generating adjusted p-values.

This is a convenience function that wraps STdiff_check_convergence and provides
a simplified output focused on p-values for downstream analysis.
}
\examples{
\dontrun{
# Extract spatial p-values for DE gene ranking
spatial_pvals = STdiff_extract_spatial_pvalues(
  sp_models=spatial_res$sp_models,
  meta_dict=prep$meta_dict,
  combo_df=prep$combo_df,
  pairwise=FALSE,
  pval_adj='fdr'
)
# View top spatial DE genes
head(spatial_pvals[order(spatial_pvals$exp_adj_p_val), ], 10)
}
}
\keyword{extraction}
\keyword{p-values}
\keyword{spatial}
\keyword{statistics}
