% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat_conversion.R
\name{as.STlist.Seurat}
\alias{as.STlist.Seurat}
\title{Convert Seurat Object to STlist}
\usage{
as.STlist.Seurat(
  x,
  assay = "RNA",
  slot = "counts",
  use.spatial = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{a Seurat object to convert}

\item{assay}{which assay to use for counts (default: 'RNA')}

\item{slot}{which slot to use for counts (default: 'counts')}

\item{use.spatial}{logical, whether to extract spatial coordinates}

\item{verbose}{logical, whether to print progress messages}
}
\value{
an STlist object
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Converts a Seurat object (particularly spatial Seurat objects) to STlist format.
}
\details{
This function extracts data from a Seurat object and creates an STlist:
\itemize{
\item Counts from the default assay (usually 'RNA' or 'SCT')
\item Spatial coordinates from images slot (for Visium/Visium HD)
\item Sample metadata from Seurat meta.data
}

Works with both single-sample and multi-sample Seurat objects.
}
\examples{
\dontrun{
# Convert Seurat to STlist
st_obj <- as.STlist.Seurat(seurat_obj)

# Use in spatialGE workflow
st_obj <- SThet(st_obj, genes = c("GENE1", "GENE2"))
compare_SThet(st_obj)
}

}
\seealso{
\code{\link{as.Seurat.STlist}}
}
