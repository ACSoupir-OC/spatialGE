% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/STenrich_core.R
\name{STenrich_core}
\alias{STenrich_core}
\title{STenrich_core: Core workflow for spatial enrichment analysis}
\usage{
STenrich_core(
  x,
  samples,
  gene_sets,
  score_type = "avg",
  reps = 1000,
  annot = NULL,
  domain = NULL,
  num_sds = 1,
  min_units = 20,
  min_genes = 5,
  pval_adj_method = "BH",
  seed = 12345,
  cores = NULL,
  verbose = TRUE
)
}
\arguments{
\item{x}{an STlist with transformed gene expression}

\item{samples}{a vector with sample names or indexes to run analysis}

\item{gene_sets}{a named list of gene sets to test}

\item{score_type}{Controls how gene set expression is calculated. Options: 'avg' or 'gsva'}

\item{reps}{the number of random samples to be extracted. Default is 1000 replicates}

\item{annot}{name of the annotation within \code{x@spatial_meta} containing spot/cell categories}

\item{domain}{the domain to restrict the analysis}

\item{num_sds}{number of standard deviations to set minimum gene set expression threshold (default: 1)}

\item{min_units}{Minimum number of spots with high expression (default: 20)}

\item{min_genes}{the minimum number of genes of a gene set present in data (default: 5)}

\item{pval_adj_method}{the method for multiple comparison adjustment (default: 'BH')}

\item{seed}{the seed number for random sampling (default: 12345)}

\item{cores}{the number of cores for parallelization. If NULL, auto-detected}

\item{verbose}{logical, whether to print text to console}
}
\value{
list of data frames with p-values and adjusted p-values per sample
}
\description{
Main workflow function that orchestrates the enrichment analysis
}
\details{
This function performs the core workflow:
\enumerate{
\item Prepare data (extract tissue spots, coordinates, match gene sets)
\item Calculate gene set expression (average or GSVA)
\item Perform permutation testing
\item Format results with adjusted p-values
}
}
\keyword{internal}
